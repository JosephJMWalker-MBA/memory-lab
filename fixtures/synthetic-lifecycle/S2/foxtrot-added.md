# Foxtrot Added

category: synthetic
tags: add, lifecycle

This source is introduced only after the baseline snapshot. It carries
metadata-like front matter lines as plain Markdown so fixture consumers can
verify metadata handling only where the imported pipeline supports it.
